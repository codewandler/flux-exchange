{
  "name": "flux-exchange-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The public documentation site for flux-exchange.",
  "license": "MIT OR Apache-2.0",
  "engines": {
    "node": ">=22.18"
  },
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "test": "node --test test/*.test.mjs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
